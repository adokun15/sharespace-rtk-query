//import { useGetUserQuery } from "../../store/Slices/user";
//import MatchForm from "../../components/MatchForm";
import Card from "../../UI/Card";
//import DataError from "../../components/DataError";

export default function FindRoommatePage() {
  /* const { data: user, error, isError, isLoading } = useGetUserQuery();

  if (isLoading) {
    return <p>Loading...</p>;
  }
  if (isError) {
    return <DataError error={error} />;
  }*/

  return (
    <Card>
      <p>Coming soon</p>
      {/*<h1 className="text-4xl text-center block">Find Match</h1>
      <p>Add your preferences and find close match. </p>
      <MatchForm user={user} />
     */}
    </Card>
  );
}
