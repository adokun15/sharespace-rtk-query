export function GetSingleChat(id) {
  return async (dispatch) => {
    //check database with specific Id
    //Provide documents
    //if successful dispatch chat list or dispatch error
  };
}

export function DeleteSingleChatAmongChats(id) {
  return async (dispatch) => {
    //check database with specific Id
    //delete element with id
    //if successful dispatch chat list or dispatch error
  };
}
