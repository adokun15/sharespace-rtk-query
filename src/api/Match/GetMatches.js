export function FindRoommatesMatchList(formData) {
  return (dispatch) => {
    //PLACE A search through User Collection expect for id givne
    //filter result with "school name" and a boolean "isOpenForMatchUp"
  };
}
